enum ActiveStatus{
    active
    banned
}
enum Role{
    customer
    technician
    admin
}

enum Status {
    failed
    pending
    completed
}

enum BookingStatus {
    pending
    accepted
    declined
    completed
}