enum ActiveStatus{
    unban
    ban
}
enum Role{
    customer
    technician
    admin
}

enum Status {
    failed
    pending
    completed
}

enum BookingStatus {
    pending
    accepted
    declined
    completed
}